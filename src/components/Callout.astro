---
export interface Props {
  type?: 'tip' | 'warn' | 'info' | 'error'
  title?: string
}

const { type = 'info', title, ...props } = Astro.props

const typeStyles = {
  tip: 'bg-green-50 border-green-200 text-green-800 dark:bg-green-900/20 dark:border-green-800 dark:text-green-200',
  warn: 'bg-yellow-50 border-yellow-200 text-yellow-800 dark:bg-yellow-900/20 dark:border-yellow-800 dark:text-yellow-200',
  info: 'bg-blue-50 border-blue-200 text-blue-800 dark:bg-blue-900/20 dark:border-blue-800 dark:text-blue-200',
  error: 'bg-red-50 border-red-200 text-red-800 dark:bg-red-900/20 dark:border-red-800 dark:text-red-200'
}

const typeIcons = {
  tip: '💡',
  warn: '⚠️',
  info: 'ℹ️',
  error: '❌'
}
---

<div class={`rounded-lg border-l-4 p-4 my-6 ${typeStyles[type]}`} {...props}>
  {title && (
    <div class="flex items-center gap-2 font-semibold mb-2">
      <span>{typeIcons[type]}</span>
      <span>{title}</span>
    </div>
  )}
  <div class="prose prose-sm max-w-none">
    <slot />
  </div>
</div>